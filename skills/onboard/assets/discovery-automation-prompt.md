Use $discover-jobs with the approved Career Pipeline configuration in the selected local workspace. Enable the durable discovery review queue and use discovery_retrieval.py for declared source scopes and full response evidence. Start by resuming actionable unfinished pages, uncaptured registered requests, missing-description pointers, unreviewed items, due verification retries, and qualifying items awaiting delivery before unrelated expansion. Register the full currently known query and board plan before the first request, including planned probes; register later-discovered queries before executing them. Immediately capture each entire native response before selecting candidates or requesting another page. Use the supported Greenhouse, Lever, and Ashby board collectors through provider exhaustion, preserving each raw page and continuation. For other enabled sources, register provider unstructured, capture the original native response, then capture the documented raw_response plus extracted_listings envelope for all identified complete records and pointers; its coverage stays unknown and incomplete. Treat the current capped Indeed connector as supplementary and leave its incomplete coverage visible. A recorded permanent provider limitation permits related refinement and independent complete board scopes after returned rows and actionable work are handled; do not loop forever or erase the original incomplete scope. Individually assess every returned unique review item, preserving repeats and nonmatches for explicit decisions. Retrieve missing descriptions even when the full board is incomplete. Refine queries using approved responsibilities and only safe approved hard filters; unknown salary, geography, or travel remains reviewable. Narrower queries remain separately tracked and do not close capped parent queries. Check canonical and configured legacy/email history, verify official postings, and deliver every qualifying role with explicit gaps. Generic pending labels are unfinished work; genuine blockers require observed attempts and a specific next action and retry. Validate or rebuild local indexes from canonical job folders, verify enabled sources, and preserve each source checkpoint independently. Successful source results require exact completed coverage_scope_ids and reconciled intake receipts. Use actual event timestamps. Report planned/registered source scope coverage separately from returned rows, unique source records, actual queue review items, and identity-reconciled opportunities, with a complete readable numbered report for new matches, meaningful changes, or actionable failures. A reviewed batch is not a completed search; absent retrieval evidence, omitted scopes, capped responses, unfinished pages, and unresolved pointers keep discovery incomplete. Stay quiet when nothing actionable changes.
